#include <stdio.h>

int main(){
    int cino, soma=0;
    while(1){
        scanf("%d", &cino);
        if(cino == 2018){
            break;
            return soma;
        } else {
            ++soma;
            continue;
            return soma;
        } 
    }
    printf("%d", soma);
    return 0;
}