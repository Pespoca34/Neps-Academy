#include <stdio.h>
#include <stdlib.h>

int main(){
    //int *p;
    //! Esqueçer de declarar, P = (int*)malloc(10*sizeof(int));
    //? usar if(p == null{return -1} else{for(int i=0; i < 10;++i){scanf("%d", &p[i])}}
    //* Esqueçer de liberar a memoria alocada, free(p);
    char *p;
    //quero imprimir Neps = 4letras
    //p = (char*)malloc(sizeof(char)*5);, aloca sempre letras + 1, por causa do '\0'
    


}